export interface indexQuerystring {
    // ID of the object to fetch
    id?: string[];
    freeForm?: {};
}
export interface indexParams {
    // username to fetch
    username: string;
}
export interface indexHeaders {
    // token to be passed as a header
    token: number[];
}
