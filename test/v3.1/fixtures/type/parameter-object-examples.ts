export interface getUserQuerystring {
    // ID of the object to fetch
    id?: string[];
    freeForm?: {};
}
export interface getUserParams {
    // username to fetch
    username: string;
}
export interface getUserHeaders {
    // token to be passed as a header
    token: number[];
}
